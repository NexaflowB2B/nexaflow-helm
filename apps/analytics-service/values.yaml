namespace: ""
appName: "analytics-service"
replicaCount: 1
image:
  repository: "ghcr.io/nexaflowb2b/analytics-service"
  tag: "latest"
  pullPolicy: IfNotPresent
service:
  name: "analytics-service"
  port: 8006
config:
  map: {}
secret:
  create: true
  sealedData:
    DATABASE_URL: "" # Paste your kubeseal output here
    SECRET_KEY: "" # Paste your kubeseal output here
env:
  - name: SECRET_KEY
    valueFrom:
      secretKeyRef:
        name: analytics-service-secret
        key: SECRET_KEY
  - name: DATABASE_URL
    valueFrom:
      secretKeyRef:
        name: analytics-service-secret
        key: DATABASE_URL
  - name: ALGORITHM
    value: "HS256"
dbInit:
  enabled: false
  command:
    - python
    - initdb.py
  backoffLimit: 6
  activeDeadlineSeconds: 600


imagePullSecrets:
  - name: ghcr-login
