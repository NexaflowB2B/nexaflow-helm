apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: dev
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: tcp-postgresql
  selector:
    app: postgres
