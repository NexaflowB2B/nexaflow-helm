{{- if .Values.config.map }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.appName }}-config
  namespace: {{ .Values.namespace }}
data:
  {{- toYaml .Values.config.map | nindent 2 }}
{{- end }}
