apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.appName }}-secret
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "nexaflow.labels" . | nindent 4 }}
spec:
  encryptedData:
    # ACTION: Replace this placeholder with your output from 'kubeseal'
    SECRET_KEY: {{ .Values.secret.sealedData.SECRET_KEY | default "Ag...PLACEHOLDER..." | quote }}
    GOOGLE_GENAI_API_KEY: {{ .Values.secret.sealedData.GOOGLE_GENAI_API_KEY | default "Ag...PLACEHOLDER..." | quote }}
